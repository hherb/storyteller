[project]
name = "mflux-test"
version = "0.1.0"
description = "MFLUX image generation prototype for Storyteller"
requires-python = ">=3.10"
dependencies = [
    "mflux>=0.9.0",
]

[tool.uv]
dev-dependencies = []
