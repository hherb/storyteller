# MFLUX - MLX-based FLUX image generation for Apple Silicon
# Note: This only works on macOS with Apple Silicon (M1/M2/M3/M4)
#
# Recommended installation method:
#   uv tool install --upgrade mflux --prerelease=allow
#
# Or via pip:
#   pip install mflux

mflux>=0.9.0
